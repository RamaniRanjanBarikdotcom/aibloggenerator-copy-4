// App bootstrap configuration.
// Fill these once before building the .exe so end users are not asked at startup.
module.exports = {
  serverApiBaseUrl: 'http://3.121.71.76/ls/api/v1/server-config/blog-gen',
  serverApiTimeoutMs: 15000,
};
